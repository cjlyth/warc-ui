'use strict';

/* jasmine specs for controllers go here */

describe('controllers', function(){
  beforeEach(module('warcUI.controllers'));


  it('MyCtrl1 should ....', inject(function() {
    //spec body
  }));

  it('MyCtrl2 should ....', inject(function() {
    //spec body
  }));
});
